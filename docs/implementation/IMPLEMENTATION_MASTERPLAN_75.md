# ShipSeal Implementation Master Plan and Milestone Ledger

Status: canonical implementation sequence

Last updated: 2026-08-07

Product direction is canonical in [Positioning](../vision/POSITIONING.md). The [Repository Intelligence PR Specification](REPOSITORY_INTELLIGENCE_PR_SPEC.md) governs its shipped domain. The [Repository Futures Product and Interaction Specification](SHIPSEAL_OMEGA_18_5_REPOSITORY_FUTURES_PRODUCT_INTERACTION_SPEC.md) governs Ω.18.5 after human acceptance.

The historical filename is retained to avoid breaking links. “75%” is no longer a claim about current completion.

## Guiding principle

Turn deterministic repository evidence into knowledge, reviewed improvements, truthful verification and one coherent executable future without executing imported code or overstating lifecycle state.

## Completed historical foundation

Historical sprint and schema names remain unchanged even where later planning reused a number.

- **Omega 6–15:** Repository DNA, Repository Intelligence foundations, Mental Model, Living Repository, Repository Universe, actionable Optimization Plan, Optimization Pack apply preparation and session-local rescan verification.
- **Omega 16.0–16.6c:** deterministic JS/TS evidence/context, provider-neutral Deep Intelligence contracts, repository-specific artifacts, reviewed Repository Intelligence PR apply and conservative artifact/statement verification.
- **Omega 17.1–17.3 and later 17.x evolution:** four post-scan chapters, modular/lazy Result Workspace, progressive disclosure, full-bleed Repository Universe, mobile/overlay behavior and truthful proposal-to-verification language.
- **Omega P1:** optional production OpenAI-compatible Deep Intelligence transport behind the provider-neutral boundary, with strict validation and deterministic fallback.
- **Historical Omega 18.1 — account/persistence foundation:** GitHub OAuth account identity, opaque database sessions, private owner-scoped PostgreSQL projects, immutable scan snapshots and versioned verification rows.
- **Later Ω.18.1 / Ω.18.1a — actionable improvement and prepared review:** friction-to-artifact lifecycle, explicit preparation and responsive plan review. This duplicate number is historical and is not retroactively renamed.
- **Ω.18.2 — PR Preview and Apply Hardening:** prepared snapshots, preflight, exact preview/base binding, bounded diffs, explicit confirmation and stage-aware recovery.
- **Deep Intelligence productionization:** bounded redacted provider input, validated future-direction findings, confidence controls and safe fallback.
- **Ω.18.4 — Authoritative Rescan Verification domain:** owner/project/repository-scoped relationship contracts, compatible evidence, statement/graph outcomes and bounded opportunity signals.
- **Post-Ω.18.4 NodeNext hotfix:** explicit server-reachable ESM specifiers and the `typecheck:functions` release gate.

Repository Universe is the primary post-scan signature experience for repository truth. Its WebGL renderer is progressive, and a complete DOM/accessibility path remains mandatory. It is not optional product decoration.

## Active program: Ω.18.5 Repository Futures

The permanent invariant is:

> One primary path + zero to two compatible supporting goals + automatically included required dependencies = one coherent Executable Future Plan.

Ω.18.5a was accepted at `29ebf3f`; implementation proceeds through the frozen a–g sequence without redefining the accepted product contract.

### Ω.18.5a — Accepted specification and terminology

Documentation only: canonical terminology, current/future grammar, versioned conceptual contracts, Quick Path, Deep Configuration, synthesis, cinematic semantics, lifecycle, outputs, accessibility, performance budgets, implementation gaps and acceptance matrix.

Exit: human accepts the canonical specification and this a–g sequence. No TypeScript, UI, migration, test or mutation change belongs here.

### Ω.18.5b — Deterministic Future Graph

Implemented on 2026-08-07 as the pure `shipseal.repository-future-graph.v1` domain: versioned schemas, strict adapters, normalization, stable identities, qualitative eligibility and fit, explicit capability dependencies, complete-cycle detection, conservative conflicts, deterministic ordering/fingerprints and focused tests.

Supported inputs are actionable improvements and deterministic transformation evidence, Repository Health, Workspace Story, already-validated Deep Intelligence `future-direction` findings, and caller-scoped eligible verified opportunity signals. Deep Intelligence remains optional and non-authoritative; opportunity-signal production wiring remains deferred to Ω.18.5f.

No Quick Path selection, UI, persistence, migration, Future artifact or prompt-pack generation, GitHub mutation, verification repair or unlock persistence is implemented in this slice.

### Ω.18.5c — Quick Path synthesis

Implement explicit one-primary selection, the two-supporting cap, automatic dependency closure, deterministic ranking, qualitative trade-offs, Save for later and one executable draft over the Ω.18.5b engine.

### Ω.18.5d — Neural field and Deep Configuration

Implement the versioned Future overlay in the existing Universe scene, semantic motion, shared Quick/Deep draft, inspectors, filters, full DOM equivalent, keyboard/touch/mobile behavior and reduced motion. Do not create a second current graph or renderer authority.

### Ω.18.5e — Future artifacts and prompt packs

Implement the canonical Future JSON/Markdown plan, artifact/evidence manifests, verification plan, reviewed repository artifacts, Codex/Claude Code/Cursor/Windsurf/human prompt packs and the immutable prepared snapshot. Reuse validated Repository Intelligence generators.

### Ω.18.5f — Persistence, apply, rescan and unlock

Implement owner-scoped Future drafts/history, prepared-plan immutability, apply-protocol convergence, exact preview/base binding, production wiring of the comprehensive authoritative relationship, measurement eligibility, persisted opportunity lineage and strict verified-only unlocks.

### Ω.18.5g — Performance, accessibility, security and release

Enforce bundle/interaction budgets, WebGL/GPU fallbacks, accessibility QA, ownership/security/migration review, full validation, dogfood export/apply/rescan/unlock flow and production smoke testing.

## Current implementation gaps and ownership

| Gap | Current truth | Owner |
| --- | --- | --- |
| Future domain | The pure versioned Future Graph foundation is implemented; executable draft selection and synthesis remain absent. | Ω.18.5c |
| One-plan interaction | No Quick Path, shared draft or Future compatibility engine exists. | Ω.18.5c |
| Future visualization | Universe has current/proposed/verification overlays, not a Future Pathways overlay. | Ω.18.5d |
| Future outputs | No canonical Future plan/manifests/prompt packs exist. | Ω.18.5e |
| Apply convergence | Optimization has stronger preview/base binding; Repository Intelligence has stronger provenance/handwritten preservation. | Ω.18.5f |
| Future persistence | Accounts/projects/scans persist; Future drafts, prepared plans, saved branches and unlock lineage do not. | Ω.18.5f |
| Measurement eligibility | `measurementCompatible` contributes a reason but is not included in final authoritative eligibility. | Ω.18.5f |
| Production relationship wiring | The comprehensive relationship builder is test-covered but not wired losslessly through production persistence/UI. | Ω.18.5f |
| Opportunity unlock safety | Pure signals are not first-class persisted Future inputs and can currently unlock from a regressed overall state. | Ω.18.5f |
| Release proof | Frozen lazy-loading, gzip, interaction, accessibility, security and end-to-end gates are not yet exercised for Futures. | Ω.18.5g |

## Deferred commercial and platform scope

Payment/entitlements, public sharing/badges, teams/organizations, scheduled or webhook rescans, automatic merge, direct default-branch writes, cross-repository Futures, several simultaneous final plans, command execution, numeric ROI/effort/token/time/success predictions, autonomous provider selection and automatic cleanup of partial GitHub branches remain deferred.

Do not imply that account persistence also means these deferred capabilities exist.

## Release gates

Repository Futures ships only when:

- deterministic evidence remains authoritative and provider output remains bounded untrusted enrichment;
- one primary, at most two supports and complete required closure are enforced in domain and UI;
- current Universe truth never becomes a copied Future graph;
- Prepared, Exported, Applied to review branch, PR opened, Pending verification and verification outcomes remain distinct;
- ZIP/manual and GitHub derive from one immutable prepared snapshot;
- GitHub writes stay explicit, branch-only, server-authorized and recoverable;
- verification requires compatible owner/project/repository/branch/scanner/measurement/plan/evidence identity;
- unresolved, incompatible or regressed relationships cannot unlock a Future;
- the complete workflow works without WebGL, hover, motion, color or a mouse;
- gzip, interaction, NodeNext, browser-marker, secret, migration, test, lint, build and human-review gates pass;
- Delivery Pack, Client Handoff, reports, manifests and `score.json` remain truthful compatibility outputs;
- applicable client-facing compliance material retains **This is not legal advice.**
